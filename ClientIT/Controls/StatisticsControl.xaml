<UserControl
    x:Class="ClientIT.Controls.StatisticsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClientIT.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">

        <!-- Loading Overlay -->
        <ProgressRing IsActive="{x:Bind IsLoading, Mode=OneWay}" 
                      HorizontalAlignment="Center" VerticalAlignment="Center" 
                      Width="60" Height="60" Canvas.ZIndex="999"/>

        <ScrollViewer Padding="24">
            <StackPanel Spacing="32">

                <!-- ========================== -->
                <!-- SEZIONE 1: REAL-TIME STATS -->
                <!-- ========================== -->
                <StackPanel Spacing="16">

                    <!-- Header Sezione -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Dashboard Real-Time" Style="{ThemeResource TitleTextBlockStyle}"/>
                            <TextBlock Text="Situazione attuale dei ticket aperti" Opacity="0.6"/>
                        </StackPanel>
                        <Button Grid.Column="1" Click="BtnRefresh_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72C;"/>
                                <TextBlock Text="Aggiorna Dati"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- 3 Card KPI (Aperti, In Corso, Chiusi) -->
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Card 1 -->
                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Aperti" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountOpen, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            </StackPanel>
                        </Border>

                        <!-- Card 2 -->
                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="In Corso" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountInProgress, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="Orange"/>
                            </StackPanel>
                        </Border>

                        <!-- Card 3 -->
                        <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Terminati (Totali)" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountClosed, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Grafici Real-Time (Torte) -->
                    <Grid ColumnSpacing="16" MinHeight="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Urgenza -->
                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Urgenza (Attivi)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind UrgencySeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Tipologia -->
                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Tipologia (Attivi)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind TypeSeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Stati (SLA) -->
                        <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Stato Attuale" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind StatusSeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </StackPanel>

                <!-- Separatore -->
                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>

                <!-- ========================== -->
                <!-- SEZIONE 2: REPORT STORICO  -->
                <!-- ========================== -->
                <StackPanel Spacing="16">

                    <!-- Header Report -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Reportistica Storica" Style="{ThemeResource TitleTextBlockStyle}"/>
                            <TextBlock Text="Analisi su range di date (Data Creazione)" Opacity="0.6"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom">
                            <CalendarDatePicker x:Name="DateStart" Header="Dal" PlaceholderText="Data inizio"/>
                            <CalendarDatePicker x:Name="DateEnd" Header="Al" PlaceholderText="Data fine"/>
                            <Button Content="Filtra" Style="{ThemeResource AccentButtonStyle}" VerticalAlignment="Bottom" Click="BtnFilter_Click"/>
                        </StackPanel>
                    </Grid>

                    <!-- KPI Report -->
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Tempo Medio -->
                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Tempo Medio Chiusura" Opacity="0.6" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE916;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                    <TextBlock Text="{x:Bind AvgCloseTime, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="Ore stimate (da apertura a 'Terminato')" FontSize="11" Opacity="0.5"/>
                            </StackPanel>
                        </Border>

                        <!-- SLA Breach -->
                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Ticket Assegnati / Totali" Opacity="0.6" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE77B;" FontSize="24" Foreground="Green"/>
                                    <TextBlock Text="{x:Bind AssignmentRate, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="Percentuale ticket presi in carico nel periodo" FontSize="11" Opacity="0.5"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Grafici Report (Griglia 2x2) -->
                    <Grid RowSpacing="16" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="300"/>
                            <RowDefinition Height="300"/>
                        </Grid.RowDefinitions>

                        <!-- Urgenza Storica -->
                        <Border Grid.Row="0" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Urgenza (Nel Periodo)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportUrgencySeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Sede Storica -->
                        <Border Grid.Row="0" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Distribuzione per Sede" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportSedeSeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Tipologia Storica -->
                        <Border Grid.Row="1" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Tipologie Più Frequenti" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportTypeSeries, Mode=OneWay}" LegendPosition="Bottom" LegendTextPaint="{x:Bind LegendTextPaint, Mode=OneWay}"/>
                            </Grid>
                        </Border>

                        <!-- Utenti (Barre) -->
                        <Border Grid.Row="1" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Ticket Chiusi per Utente IT" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:CartesianChart Grid.Row="1" Series="{x:Bind ReportUserSeries, Mode=OneWay}" XAxes="{x:Bind UserXAxes, Mode=OneWay}" YAxes="{x:Bind UserYAxes, Mode=OneWay}"/>
                            </Grid>
                        </Border>
                    </Grid>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>