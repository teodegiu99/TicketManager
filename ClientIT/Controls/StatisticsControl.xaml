<UserControl
    x:Class="ClientIT.Controls.StatisticsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClientIT.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:models="using:ClientIT.Models"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <ProgressRing IsActive="{x:Bind IsLoading, Mode=OneWay}" 
                      HorizontalAlignment="Center" VerticalAlignment="Center" 
                      Width="60" Height="60" Canvas.ZIndex="999"/>

        <ScrollViewer Padding="24">
            <StackPanel Spacing="32">

                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Dashboard Real-Time" Style="{ThemeResource TitleTextBlockStyle}"/>
                            <TextBlock Text="Situazione attuale dei ticket aperti" Opacity="0.6"/>
                        </StackPanel>
                        <Button Grid.Column="1" Click="BtnRefresh_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE72C;"/>
                                <TextBlock Text="Aggiorna Dati"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Aperti" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountOpen, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="In Corso" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountInProgress, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="Orange"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Terminati (Totali)" Opacity="0.6" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind CountClosed, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid ColumnSpacing="16" MinHeight="250">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Urgenza (Attivi)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind UrgencySeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="Tipologia (Attivi)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind TypeSeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Grid.Row="0" Text="SLA Attuali" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ColorSeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>
                    </Grid>
                </StackPanel>

                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.5"/>

                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Reportistica Storica" Style="{ThemeResource TitleTextBlockStyle}"/>
                            <TextBlock Text="Analisi ticket terminati" Opacity="0.6"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Bottom">
                            <CalendarDatePicker x:Name="DateStart" Header="Dal"/>
                            <CalendarDatePicker x:Name="DateEnd" Header="Al"/>
                            <Button Content="Filtra" Style="{ThemeResource AccentButtonStyle}" VerticalAlignment="Bottom" Click="BtnFilter_Click"/>
                        </StackPanel>
                    </Grid>

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Tempo Medio Chiusura" Opacity="0.6" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE916;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                    <TextBlock Text="{x:Bind AvgCloseTime, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <StackPanel>
                                <TextBlock Text="Urgenza Modificata" Opacity="0.6" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE814;" FontSize="24" Foreground="Orange"/>
                                    <TextBlock Text="{x:Bind UrgencyChangedRate, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid RowSpacing="16" ColumnSpacing="16" MinHeight="950" VerticalAlignment="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="300"/>
                            <RowDefinition Height="300"/>
                            <RowDefinition Height="300"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Text="Urgenza (Storico)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportUrgencySeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Row="0" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Text="Distribuzione Sede" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportSedeSeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Text="Tipologia (Storico)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportTypeSeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Text="Ticket per Utente" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:CartesianChart Grid.Row="1"
                                                    Series="{x:Bind ReportUserSeries, Mode=OneWay}" 
                                                    XAxes="{x:Bind UserXAxes, Mode=OneWay}" 
                                                    YAxes="{x:Bind UserYAxes, Mode=OneWay}" 
                                                    LegendPosition="Bottom"
                                                    Background="Transparent"/>
                            </Grid>
                        </Border>

                        <Border Grid.Row="2" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Grid RowDefinitions="Auto, *">
                                <TextBlock Text="Modalità Apertura" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <lvc:PieChart Grid.Row="1" Series="{x:Bind ReportCreatorSeries, Mode=OneWay}" LegendPosition="Bottom" Background="Transparent"/>
                            </Grid>
                        </Border>

                    </Grid>
                </StackPanel>

                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.5"/>

                <StackPanel Spacing="16">
                    <TextBlock Text="Analisi per Utente" Style="{ThemeResource TitleTextBlockStyle}"/>
                    <TextBlock Text="Cerca per nome o username per vedere i dettagli" Opacity="0.6"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <AutoSuggestBox x:Name="UserSearchBox" PlaceholderText="Es: Matteo o m.rossi" QueryIcon="Find" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Cerca" Margin="8,0,0,0" Style="{ThemeResource AccentButtonStyle}" Click="SearchUser_Click"/>
                    </Grid>

                    <StackPanel x:Name="UserStatsPanel" Visibility="{x:Bind UserStatsVisible, Mode=OneWay}" Spacing="16">

                        <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,8,0,0">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE77B;" FontSize="16" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="Utente trovato" FontWeight="SemiBold"/>
                            </StackPanel>

                            <Rectangle Width="1" Height="16" Fill="{ThemeResource CardStrokeColorDefaultBrush}"/>

                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7F4;" FontSize="16" Opacity="0.6"/>
                                <TextBlock Text="Ultima Postazione:" Opacity="0.6"/>
                                <TextBlock Text="{x:Bind UserMachineName, Mode=OneWay}" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <StackPanel Spacing="12">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8D4;" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                        <TextBlock Text="Ticket Propri" FontWeight="Bold" FontSize="16"/>
                                    </StackPanel>

                                    <Grid ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Aperti / In Corso" FontSize="12" Opacity="0.6"/>
                                            <TextBlock Text="{x:Bind UserOwnOpenCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                        </StackPanel>

                                        <Rectangle Grid.Column="1" Width="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,4"/>

                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                            <TextBlock Text="Terminati" FontSize="12" Opacity="0.6" HorizontalAlignment="Right"/>
                                            <TextBlock Text="{x:Bind UserOwnClosedCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="Gray" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <StackPanel Spacing="12">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE716;" Foreground="Orange"/>
                                        <TextBlock Text="Per Conto Di Terzi" FontWeight="Bold" FontSize="16"/>
                                    </StackPanel>

                                    <Grid ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Aperti / In Corso" FontSize="12" Opacity="0.6"/>
                                            <TextBlock Text="{x:Bind UserBehalfOpenCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="Orange"/>
                                        </StackPanel>

                                        <Rectangle Grid.Column="1" Width="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="0,4"/>

                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                            <TextBlock Text="Terminati" FontSize="12" Opacity="0.6" HorizontalAlignment="Right"/>
                                            <TextBlock Text="{x:Bind UserBehalfClosedCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="Gray" HorizontalAlignment="Right"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Grid ColumnSpacing="16" RowSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <Grid ColumnDefinitions="*, Auto">
                                    <StackPanel>
                                        <TextBlock Text="Aperti da altri per te" Opacity="0.6" FontWeight="SemiBold"/>
                                        <TextBlock Text="Ticket ricevuti dal CED o colleghi" FontSize="11" Opacity="0.5"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{x:Bind UserTicketsReceivedCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="Teal" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <Grid ColumnDefinitions="*, Auto">
                                    <StackPanel>
                                        <TextBlock Text="Urgenza Modificata" Opacity="0.6" FontWeight="SemiBold"/>
                                        <TextBlock Text="Interventi su priorità errata" FontSize="11" Opacity="0.5"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{x:Bind UserUrgencyChangedCount, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="OrangeRed" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <Grid RowDefinitions="Auto, *">
                                    <TextBlock Text="Tipologie Segnalate" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <lvc:PieChart Grid.Row="1" Height="200" Series="{x:Bind UserTypeSeries, Mode=OneWay}" LegendPosition="Right" Background="Transparent"/>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <Grid RowDefinitions="Auto, *">
                                    <TextBlock Text="Distribuzione Urgenza" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <lvc:PieChart Grid.Row="1" Height="200" Series="{x:Bind UserUrgencySeries, Mode=OneWay}" LegendPosition="Right" Background="Transparent"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <TextBlock Text="Storico Ticket Creati" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,8,0,0"/>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <ListView ItemsSource="{x:Bind UserTicketList, Mode=OneWay}" 
                                      MaxHeight="300"
                                      IsItemClickEnabled="True"
                                      ItemClick="UserTicketListView_ItemClick">
                                <ListView.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid Padding="12" ColumnDefinitions="80, 150, *, 120">
                                            <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold" Opacity="0.5"/>
                                            <TextBlock Grid.Column="1" Text="Data" FontWeight="Bold" Opacity="0.5"/>
                                            <TextBlock Grid.Column="2" Text="Oggetto" FontWeight="Bold" Opacity="0.5"/>
                                            <TextBlock Grid.Column="3" Text="Stato" FontWeight="Bold" Opacity="0.5"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.HeaderTemplate>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:TicketViewModel">
                                        <Grid Padding="12" ColumnDefinitions="80, 150, *, 120">
                                            <TextBlock Grid.Column="0" Text="{x:Bind Nticket}" FontWeight="SemiBold"/>
                                            <TextBlock Grid.Column="1" Text="{x:Bind local:StatisticsControl.FormatDate(DataCreazione)}" Opacity="0.8"/>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind Titolo}" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{x:Bind PerContoDi}" FontSize="10" Foreground="Orange" Visibility="{x:Bind PerContoDiVisibility}"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="3" Text="{x:Bind StatoNome}" Opacity="0.8"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>

                    </StackPanel>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>